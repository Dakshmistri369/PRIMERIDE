{
  "name": "primeride",
  "version": "1.0.0",
  "description": "PrimeRide — A premium, high-fidelity ride-hailing landing page and web application built with Vue 3, Three.js WebGL, Express, and Supabase.",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "server": "npm run dev --prefix server",
    "start": "npm run start --prefix server"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2"
  }
}
